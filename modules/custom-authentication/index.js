/**
 * Custom authentication
 *
 * @desc some features if you want reusable to other projects we should split to new module like this
 * @author Maethee
 * */
/**
 * Strategy dependencies.
 */

module.exports = {
    NormallyStrategy: require('./strategies/normally-strategy')
};
